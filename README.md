# Product Manager

Учебный backend-проект на Python для управления товарами.

Проект создан для практики базовой серверной логики: CRUD-операций, валидации данных, обработки ошибок, работы с JSON-хранилищем, модульной структуры проекта, основ ООП и разработки REST API на FastAPI.

## Возможности

- получение списка товаров;
- поиск товара по `id`;
- создание товара;
- обновление товара;
- удаление товара;
- фильтрация доступных товаров;
- валидация входных данных;
- единый формат ответа для успешных и ошибочных операций;
- сохранение данных в JSON-файл;
- REST API на FastAPI;
- интерактивная Swagger-документация;
- демонстрационный запуск через `main.py`.

## Стек

- Python
- FastAPI
- Uvicorn
- Pydantic
- JSON
- ООП
- Git / GitHub

## Структура проекта

```text
product-manager-api/
│
├── app/
│   ├── __init__.py
│   ├── api.py
│   ├── main.py
│   ├── models.py
│   ├── schemas.py
│   ├── services.py
│   ├── storage.py
│   ├── validators.py
│   └── responses.py
│
├── data/
│   └── products.json
│
├── README.md
├── .gitignore
└── requirements.txt
```

## Описание файлов

`app/api.py` — FastAPI-приложение с REST-эндпоинтами для работы с товарами.

`app/models.py` — содержит модель товара `Product`.  
В этом файле описана структура товара и методы для обновления данных и преобразования объекта в словарь.

`app/schemas.py` — Pydantic-схемы для валидации входных данных API.

`app/services.py` — содержит сервисный слой `ProductService`.  
Здесь находится основная логика проекта: получение списка товаров, поиск по `id`, создание, обновление, удаление и фильтрация доступных товаров.

`app/storage.py` — отвечает за работу с JSON-хранилищем.  
Файл содержит функции для загрузки товаров из `data/products.json`, сохранения изменений и сброса данных к начальному состоянию для демонстрационного запуска.

`app/validators.py` — содержит функции валидации входных данных.  
Проверяется название товара, цена и количество.

`app/responses.py` — содержит единый формат ответа для успешных и ошибочных операций.

`app/main.py` — точка входа для консольного демонстрационного запуска проекта.

`data/products.json` — файл с данными о товарах.

---

## Как запустить проект

Клонировать репозиторий:

```bash
git clone https://github.com/texze2012/product-manager-api.git
```

Перейти в папку проекта:

```bash
cd product-manager-api
```

Установить зависимости:

```bash
pip install -r requirements.txt
```

На Windows также можно использовать:

```bash
py -m pip install -r requirements.txt
```

---

## Как запустить API

Запустить FastAPI-сервер:

```bash
py -m uvicorn app.api:app --reload
```

Альтернативный вариант:

```bash
uvicorn app.api:app --reload
```

После запуска открыть в браузере:

```text
http://127.0.0.1:8000/docs
```

Swagger UI позволит протестировать все эндпоинты проекта.

---

## Как запустить консольный demo-сценарий

```bash
python -m app.main
```

На Windows также можно использовать:

```bash
py -m app.main
```

---

## API-эндпоинты

- `GET /` — проверка, что API запущен;
- `GET /products` — получить список товаров;
- `GET /products/available` — получить список доступных товаров;
- `GET /products/{product_id}` — получить товар по `id`;
- `POST /products` — создать товар;
- `PUT /products/{product_id}` — обновить товар;
- `DELETE /products/{product_id}` — удалить товар.

---

## Пример запроса на создание товара

`POST /products`

```json
{
    "name": "Keyboard",
    "price": 3500,
    "quantity": 4
}
```

Пример успешного ответа:

```json
{
    "success": true,
    "data": {
        "id": 4,
        "name": "Keyboard",
        "price": 3500,
        "quantity": 4,
        "is_available": true
    },
    "error": null
}
```

---

## Пример работы demo-сценария

При запуске `main.py` выполняется демонстрационный сценарий:

- вывод списка товаров;
- создание нового товара;
- попытка создать товар с некорректной ценой;
- поиск товара по `id`;
- поиск несуществующего товара;
- вывод доступных товаров;
- обновление товара;
- удаление товара;
- вывод итогового списка товаров.

Пример вывода:

```text
=== Список товаров ===
Успех:
[...]

=== Создание товара ===
Успех:
{'id': 4, 'name': 'Keyboard', 'price': 3500, 'quantity': 4, 'is_available': True}

=== Создание товара с некорректной ценой ===
Ошибка:
Цена должна быть больше 0

=== Поиск несуществующего товара ===
Ошибка:
Товар не найден
```

---

## Формат ответа

В проекте используется единый формат ответа для всех операций.

Успешный ответ:

```python
{
    "success": True,
    "data": {...},
    "error": None
}
```

Ответ с ошибкой:

```python
{
    "success": False,
    "data": None,
    "error": "Текст ошибки"
}
```

Такой подход позволяет одинаково обрабатывать успешные и ошибочные операции.

---

## Пример товара

```json
{
    "id": 1,
    "name": "TV",
    "price": 30000,
    "quantity": 6,
    "is_available": true
}
```

Поля товара:

- `id` — уникальный идентификатор товара;
- `name` — название товара;
- `price` — цена товара;
- `quantity` — количество товара;
- `is_available` — доступность товара, рассчитывается на основе количества.

---

## Валидация данных

При создании и обновлении товара проверяются входные данные:

- название должно быть строкой и не может быть пустым;
- цена должна быть числом больше 0;
- количество должно быть целым числом и не может быть меньше 0.

Если данные некорректны, сервис возвращает ошибку в едином формате ответа.

---

## Планы развития

В дальнейшем планируется развить проект:

- улучшить обработку ошибок через HTTP-статусы;
- подключить SQLite или PostgreSQL;
- добавить Dockerfile;
- добавить базовые тесты;
- оформить примеры API-запросов;
- добавить документацию по эндпоинтам.